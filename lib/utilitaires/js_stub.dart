library;

// This is a stub file to make the analyzer happy.
// The real dart:js library is only available on the web platform.

dynamic get context => throw UnimplementedError();

class JsObject {
  JsObject.jsify(Object? data) {
    throw UnimplementedError();
  }
}

Function allowInterop<F extends Function>(F f) {
  throw UnimplementedError();
}
